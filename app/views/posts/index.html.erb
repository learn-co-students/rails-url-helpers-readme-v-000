<!-- <% @posts.each do |post| %>
  <div><a href='<%= "/posts/#{post.id}" %>'><%= post.title %></a></div>
<% end %> -->

<!-- <% @posts.each do |post| %>
  <div><%= link_to post.title, "/posts/#{post.id}" %></div> ## <a href='<%= "/posts/#{post.id}"%>'>  ==>  <%= link_to post.title, "/posts/#{post.id}" %>
<% end %> -->

<!-- <% @posts.each do |post| %>
  <div><%= link_to post.title, post_path(post.id) %></div> ## "/posts/#{post.id}"  ===> post_path(post.id)
<% end %> -->

<% @posts.each do |post| %>
  <div><%= link_to post.title, post_path(post) %></div> <!-- post_path(post.id) ==> post_path(post) -->
<% end %>
